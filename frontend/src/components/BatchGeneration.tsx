import React from 'react';
import { Box, Typography } from '@mui/material';

// Placeholder component for BatchGeneration
// This component is under development
export const BatchGeneration: React.FC = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h6" gutterBottom>
        Batch Generation
      </Typography>
      <Typography variant="body2" color="text.secondary">
        This component is under development. Batch generation functionality will be available soon.
      </Typography>
    </Box>
  );
};

export default BatchGeneration;